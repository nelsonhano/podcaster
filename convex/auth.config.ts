const authConfig = {
    providers: [
        {
            domain: "https://expert-garfish-89.clerk.accounts.dev",
            applicationID: "convex",
        },
    ]
};

export default authConfig;